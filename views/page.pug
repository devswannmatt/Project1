include partials/header

if user && user.role === 'admin'
  button.btn.btn-primary.btn-sm.pull-right(data-type='frm' data-toggle='modal', data-target='#modal', data-src=`/pages/edit/${page._id}` data-size='xl', data-name=`${page.title}`)
    i(class=`fas fa-edit`)
div.content
  != page.content
  block content

include partials/footer